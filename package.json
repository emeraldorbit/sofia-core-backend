{
  "name": "sofia-core",
  "version": "1.0.2",
  "description": "Behavioral governance engine for Sofia Core",
  "type": "module",
  "scripts": {
    "test": "deno test tests --allow-read",
    "test:verbose": "deno test tests --allow-read --trace-ops",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=18.0.0"
  }
}
